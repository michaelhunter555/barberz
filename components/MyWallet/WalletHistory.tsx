import { StyledText, StyledView, StyledBlurItem } from "../shared/SharedStyles";
import { ScrollView, } from 'react-native';

const WalletHistory = () => {
    return (
        <StyledView direction="column" style={{ flex: 1}}>
            <ScrollView>

                

            </ScrollView>
        </StyledView>
    )
}